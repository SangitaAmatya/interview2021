package WalmartCoding;

public class DuplicateElementArrayEasy {
	/*
	 * Our first solution is very simple. All we are doing here is to loop over an
	 * array and comparing each element to every other element. Since we are
	 * comparing every element to every other element, this solution has quadratic
	 * time complexity i.e. O(n^2)
	 */
	public static void main(String[] args) {
		int number[]= {1,5,2,6,6,8,5};
		//compare each elwmrny 0(nxn)//worst 
		for(int i = 0;i<number.length;i++) {
			for(int j = i+1;j<number.length;j++) {
				if(number[i].equals[number[j]{
					System.out.println(number[i]);
					
				}
				
			}
		}

	}

}
